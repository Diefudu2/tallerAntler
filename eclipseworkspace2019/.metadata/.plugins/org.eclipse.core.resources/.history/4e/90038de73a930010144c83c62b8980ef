program mixto{
    var a;
    a = DIGITALROOT(55);
    println a;                    
    if (a > 5 && a < 8) {
        println a;
    } else {
        println false;
    }
    var r;
    r = SIMULATEGAUSS(50, 10);
    println r;